<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.malangstore.dao.OrderlistDao">


    <select id="getProduct" parameterType="int" resultType="com.malangstore.beans.Product">
        SELECT product_no, product_price, product_delivery, product_name FROM product
            WHERE product_no = #{product_no}
    </select>

    <insert id="insertCart" parameterType="map">
        INSERT INTO orderlist(orderlist_no, member_id, product_name, product_no, product_price, product_delivery, orderlist_check, orderlist_date, orderlist_count)
            VALUES (NEXTVAL('seq_orderlist_no'), #{member_id}, #{product.product_name}, #{product.product_no}, #{product.product_price}, #{product.product_delivery}, 'n', CURRENT_TIMESTAMP(0), 1)
    </insert>

    <select id="cartList" parameterType="String" resultType="com.malangstore.beans.Orderlist">
        SELECT * FROM orderlist
            WHERE member_id = #{member_id}
                ORDER BY orderlist_date DESC
    </select>

    <delete id="deleteOrder" parameterType="int">
        DELETE FROM orderlist
            WHERE orderlist_no = #{orderlist_no}
    </delete>
</mapper>